<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="per.codeant.common.proxy.ConfigInfoAggrMapper">

	<insert id="batchInsert" useGeneratedKeys="true" keyProperty="id" parameterType="java.util.List">
		INSERT INTO config_info_aggr
			 (
				id,
				data_id,
				group_id,
				datum_id,
				content,
				gmt_modified,
				app_name,
				tenant_id
			) VALUES 
		<foreach collection="list" item="record" separator="," index="index">
			(
				#{record.id},
				#{record.dataId},
				#{record.groupId},
				#{record.datumId},
				#{record.content},
				#{record.gmtModified},
				#{record.appName},
				#{record.tenantId}
			)
		</foreach>
	</insert>
</mapper>